# ProofCast Web App

This app contains the ProofCast frontend and Next.js route-handler backend.

Important folders:

- `app`: routes and API handlers
- `features`: product-specific client components
- `server`: backend services for Tatum, Walrus, AI, Redis, Prisma, and verification
- `prisma`: database schema
- `components`: shared UI
